- name: gp
  configMap:
    name: gpdomainproxy
- name: data
  persistentVolumeClaim:
    claimName: data
- name: dumps
  persistentVolumeClaim:
    claimName: hcdumps
- name: mdt
  persistentVolumeClaim:
    claimName: mdt
