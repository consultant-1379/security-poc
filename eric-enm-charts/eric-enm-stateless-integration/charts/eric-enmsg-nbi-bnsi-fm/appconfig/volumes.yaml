- name: gp
  configMap:
    name: gpnbi-bnsi-fm
- name: data
  persistentVolumeClaim:
    claimName: data
- name: mdt
  persistentVolumeClaim:
    claimName: mdt
- name: batch
  persistentVolumeClaim:
    claimName: batch
- name: configmgt
  persistentVolumeClaim:
    claimName: configmgt
- name: upgind
  persistentVolumeClaim:
    claimName: upgind
- name: custom
  persistentVolumeClaim:
    claimName: custom
- name: dumps
  persistentVolumeClaim:
    claimName: hcdumps
- name: pmic1
  persistentVolumeClaim:
    claimName: pm1
- name: pmic2
  persistentVolumeClaim:
    claimName: pm2
- name: pmlinks
  persistentVolumeClaim:
     claimName: pmlinks
