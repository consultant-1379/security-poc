- name: gp
  configMap:
    name: gpsso
- name: data
  persistentVolumeClaim:
    claimName: data
- name: mdt
  persistentVolumeClaim:
    claimName: mdt
- name: custom
  persistentVolumeClaim:
    claimName: custom
- name: hcdumps
  persistentVolumeClaim:
    claimName: hcdumps
- name: dlms
  persistentVolumeClaim:
    claimName: dlms
- name: batch
  persistentVolumeClaim:
    claimName: batch
- name: norollback
  persistentVolumeClaim:
    claimName: norollback
- name: configmgt
  persistentVolumeClaim:
    claimName: configmgt
- name: home
  persistentVolumeClaim:
    claimName: home
- name: upgind
  persistentVolumeClaim:
    claimName: upgind
