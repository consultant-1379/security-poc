- name: gp
  configMap:
    name: gpcloud
- name: data
  persistentVolumeClaim:
    claimName: data
- name: dumps
  persistentVolumeClaim:
    claimName: hcdumps
- name: mdt
  persistentVolumeClaim:
    claimName: mdt
- name: shared
  persistentVolumeClaim:
    claimName: home
- name: custom
  persistentVolumeClaim:
    claimName: custom
- name: norollback
  persistentVolumeClaim:
    claimName: norollback
- name: cron
  persistentVolumeClaim:
    claimName: cron
- name: enm-version-configmap
  configMap:
    name: eric-enm-version-configmap
