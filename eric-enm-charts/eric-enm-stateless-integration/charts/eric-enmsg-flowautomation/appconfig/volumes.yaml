- name: gp
  configMap:
    name: gpflowautomation
- name: data
  persistentVolumeClaim:
    claimName: data
- name: upgind
  persistentVolumeClaim:
    claimName: upgind
- name: dumps
  persistentVolumeClaim:
    claimName: hcdumps
- name: mdt
  persistentVolumeClaim:
    claimName: mdt
- name: norollback
  persistentVolumeClaim:
    claimName: norollback
