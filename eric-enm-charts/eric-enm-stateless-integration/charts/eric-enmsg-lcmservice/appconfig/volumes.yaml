- name: gp
  configMap:
    name: gplcmserv
- name: data
  persistentVolumeClaim:
    claimName: data
- name: dumps
  persistentVolumeClaim:
    claimName: hcdumps
- name: mdt
  persistentVolumeClaim:
    claimName: mdt
- name: shared
  persistentVolumeClaim:
    claimName: home
- name: custom
  persistentVolumeClaim:
    claimName: custom
- name: dlms
  persistentVolumeClaim:
    claimName: dlms
- name: norollback
  persistentVolumeClaim:
    claimName: norollback
- name: batch
  persistentVolumeClaim:
    claimName: batch
- name: configmgt
  persistentVolumeClaim:
    claimName: configmgt
- name: pm1
  persistentVolumeClaim:
    claimName: pm1
- name: pm2
  persistentVolumeClaim:
    claimName: pm2
- name: pmlinks
  persistentVolumeClaim:
    claimName: pmlinks
- name: upgind
  persistentVolumeClaim:
    claimName: upgind
- name: smrs
  persistentVolumeClaim:
    claimName: smrs
- name: cron
  persistentVolumeClaim:
    claimName: cron
