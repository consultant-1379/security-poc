- name: gp
  configMap:
    name: gpcloud
- name: data
  persistentVolumeClaim:
    claimName: data
- name: upgind
  persistentVolumeClaim:
    claimName: upgind
- name: dumps
  persistentVolumeClaim:
    claimName: hcdumps
- name: mdt
  persistentVolumeClaim:
    claimName: mdt
- name: custom
  persistentVolumeClaim:
    claimName: custom
- name: norollback
  persistentVolumeClaim:
    claimName: norollback
