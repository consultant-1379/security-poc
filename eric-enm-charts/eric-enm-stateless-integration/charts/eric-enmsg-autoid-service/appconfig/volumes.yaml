- name: gp
  configMap:
    name: gpautoidservice
- name: data
  persistentVolumeClaim:
    claimName: data
- name: dumps
  persistentVolumeClaim:
    claimName: hcdumps
- name: mdt
  persistentVolumeClaim:
    claimName: mdt
- name: norollback
  persistentVolumeClaim:
    claimName: norollback
- name: dlms
  persistentVolumeClaim:
    claimName: dlms
- name: custom
  persistentVolumeClaim:
    claimName: custom
- name: home
  persistentVolumeClaim:
    claimName: home
- name: configmgt
  persistentVolumeClaim:
    claimName: configmgt
- name: pm1
  persistentVolumeClaim:
    claimName: pm1
- name: pm2
  persistentVolumeClaim:
    claimName: pm2
- name: pmlinks
  persistentVolumeClaim:
    claimName: pmlinks
- name: batch
  persistentVolumeClaim:
    claimName: batch
- name: upgind
  persistentVolumeClaim:
    claimName: upgind
