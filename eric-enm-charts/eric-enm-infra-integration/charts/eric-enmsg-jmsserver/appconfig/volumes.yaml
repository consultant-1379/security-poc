- name: pv-gp
  configMap:
    name: gpcloud
- name: pv-data
  persistentVolumeClaim:
    claimName: data
- name: pv-mdt
  persistentVolumeClaim:
    claimName: mdt
- name: pv-dumps
  persistentVolumeClaim:
    claimName: hcdumps
- name: jms
  persistentVolumeClaim:
    claimName: jms
